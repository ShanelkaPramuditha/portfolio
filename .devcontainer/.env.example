# Copy this to .devcontainer/.env and fill in your values.
# These are injected into the devcontainer at build time via remoteEnv.

# Git identity - used by onCreateCommand to configure git inside the container
GIT_USER_EMAIL=you@example.com
GIT_USER_NAME=Your Name
